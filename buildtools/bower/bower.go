package bower

// see e.g. new analyzer spec (we can probably bring `modules`, `graph`, etc. to top-level)
// the key here is to make the parsing functions pure so we can unit test them with testdata

func ParseStrategyOne() {}

func ParseStrategyTwo() {}

func Build() {}

func Analyze() {}
